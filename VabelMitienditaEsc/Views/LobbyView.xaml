<UserControl x:Class="VabelMitienditaEsc.Views.LobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VabelMitienditaEsc.Views"
             mc:Ignorable="d"
             Background="{StaticResource BrushBackgroundWhite}"
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrushBackgroundWhite}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Color="#000000" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <StackPanel>
                    <TextBlock Text="{Binding WelcomeMessage}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushPrimaryBlue}"/>
                    <TextBlock Text="Este es el sistema Vabel para gestionar tu tiendita. Seleccione una operación en el menú lateral para comenzar." FontSize="18" Foreground="#7F8C8D" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
        </Grid>
    </Grid>
</UserControl>
